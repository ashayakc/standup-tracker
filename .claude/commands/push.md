Stage all changed files (but skip secrets, .env files, and large binaries), create a commit with a concise message describing the changes, and push to the current branch on origin.

Report the commit hash and branch name when done.
